import { Meta } from "@storybook/blocks";

<Meta title="Storybook for Tailwind Blog" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Storybook for Tailwind Blog

    This is the component library for my Next.js Tailwind Blog portfolio.

    It was set up as an example of an npm component package that can be installed into the main project.

  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }
  `}
</style>
